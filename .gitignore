# Repo for GitHub Pages — deployed via GitHub Actions (no build artifacts in git).
# Patterns starting with `/` are anchored to the repo root so they don't

# Top-level directories not needed
/.claude/
/mcp/
/plans/
/docs/
# Node / Astro build artifacts
ui/node_modules/
ui/dist/
ui/.astro/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
